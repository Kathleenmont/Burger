<h1>Eat Dat Burger</h1>

<h2>Burgers to Be Eaten!</h2>

<img src="http://localhost:8080/static/assets/img/burger.png" width="300px">

<ul id="toEat">
  {{#each burgers}}
    {{#unless devoured}} 
      {{> burgers/burger-button devoured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers you have  already eaten!</h2>
<ul id="eaten">
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-button devoured=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add A Burger!</h2>

<form class="create-form">
  <div class="form-group">
    <label>Burger Name:</label>
    <input type="text" id="newBurger" name="burger_name">
  </div>
  
  <button type="submit" id="add-burger">Add Burger</button>
</form>


{{!-- <h2>Add a Cat</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Cat Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Sleepy?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Sleepy!<br>
    <input type="radio" name="sleepy" value="0"> Awake
  </div>

  <button type="submit">Add Cat</button>
</form>   --}}